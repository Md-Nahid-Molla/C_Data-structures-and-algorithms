#include <stdio.h>
int main()
{
  printf("size of int : %d",sizeof(int));
  printf("size of float : %f\n",sizeof(float));
  printf("size of dubale : %if\n",sizeof(dubale));
  printf("size of char : %c\n",sizeof(char));
  return 0;
}
